# Copy to .env (gitignored).
# HF_TOKEN=hf_...
# CONFIG=grpo-qwen3-8b.yaml
# IMAGE=verlai/verl:vllm023.aarch64.dev1   # newest STABLE arm64 tag
# VERL_VERSION=v0.9.0                       # the source pin; both nodes should match
# VERL_SRC=~/src/verl                       # where the checkout lives
# DATA_DIR=data/gsm8k                       # ws up ray-verl --prepare-data builds it
