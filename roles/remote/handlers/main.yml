---
# Restart ssh.service, not ssh.socket: under socket activation the service is
# what holds the sshd config. Existing connections survive a restart.
- name: Restart ssh
  ansible.builtin.systemd:
    name: ssh.service
    state: restarted
  become: true
